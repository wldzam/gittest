#include <chrono>
#include <iostream>
#include <thread>
using namespace std;

int main()
{

    int i;
    cin >> i;
    if (i > 10) {
        cout << "ok" << endl;
    } else {
        cout << "bad" << endl;
    }














    //this_thread::sleep_for(chrono::seconds(1));

    return 0;
}
